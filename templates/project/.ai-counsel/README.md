# Project template for using AI Counsel MCP

Create this folder inside your project repo:

.ai-counsel/
  env          # secrets (API keys), not committed
  config.yaml  # optional override, not committed

Then configure your MCP client to call: `mcp-ai-counsel`
See docs/USAGE.md in the toolbox repo.
